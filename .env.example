# Optional for legacy database-backed routes. Transit Signals does not use MongoDB.
MONGO_URL=
JWT_SECRET=replace-with-a-long-random-secret
PORT=3001

# Transit Signals
SOCRATA_APP_TOKEN=
# TRANSIT_DATA_DIR=../client/public/data/transit
